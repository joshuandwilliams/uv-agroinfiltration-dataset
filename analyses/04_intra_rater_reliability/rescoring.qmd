Plan:

- Load full dataset
- Create dataset without CDAs whose scores range by 3+
- Subset dataset for non-0 median scores
- For each individual, randomly sample 180 CDAs that they've already scored (30 from each class) -> randomise order and save CSV files
- Create PDF documents where each page contains 20 (4x5) CDA images with dotted lines for writing
- Create PDF documents with duplicate images but with the filename and median score labelled
- Print out these booklets and bring to lab meeting on Tuesday with a pack of pens.
- Have each scorer score their booklet.
- Enter results into computer. Compare original scores to rescores and calculate % for each scorer